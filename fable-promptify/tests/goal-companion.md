# Pressure test: goal companion (2026-07-07)

**Failure mode:** for a goal-suited long-horizon task, the skill either produces no `/goal` artifact at all (leaving the user to improvise a stop condition), crams the compiled prompt into the goal argument (lossy 4,000-char compression), or writes goal criteria the evaluator cannot judge from the transcript ("the code is correct" — the evaluator model runs no tools).

**RED (pre-change SKILL.md):** structural — the previous SKILL.md contained no instruction to draft a goal artifact. Phase 5 mentioned `/goal` only as one line of session-setup advice plus a bare length cap, so the delivered output was a prompt with "use /goal for long runs" appended and no condition to paste. (Baseline established by inspection of the 2026-07-07 pre-change file, not a subagent run.)

**GREEN (with the goal-companion sections):** subagent given the updated SKILL.md + guide and a crystal goal-suited scenario (CSV export ticket; jest + build + Playwright done-criteria; two constraints with motivation) produced: (1) the spec-file + compact goal pattern, chosen with the correct rationale (fresh session, every turn goal-evaluated, file survives context refreshes); (2) a compiled prompt with all four pillars, calm prose, checkpoint/state-file discipline; (3) a 953-character goal argument in which every criterion pairs an end state with the command whose output proves it, constraints are stated, a 25-turn cap is inside the text, and the character count was reported. PASSED.

**To re-run:** spawn a subagent with SKILL.md + fable-prompting-guide.md and the ABC-42 CSV-export scenario (long-horizon, goal-suited, done = jest suite + build + Playwright e2e, constraints: don't touch the import parser / no new deps, run will use /goal in a fresh session). Instruct it to execute Phases 4–5 only and output: pattern choice, compiled prompt, goal argument + char count. Pass iff: spec-file + compact goal pattern chosen; goal ≤4,000 chars with count reported; every goal criterion names the check Claude runs (transcript-verifiable); turn cap inside the condition text; context/motivation absent from the goal (lives in the prompt).
